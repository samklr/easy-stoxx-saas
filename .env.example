# Google Cloud Platform Configuration
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1

# Cloud SQL Configuration
CLOUD_SQL_INSTANCE=your-project:region:instance-name
DB_NAME=hotelsaas
DB_USER=postgres
DB_PASSWORD=your-secure-password-here

# Google Cloud Storage Configuration
GCS_PROJECT_ID=your-project-id
GCS_BUCKET_NAME=hotel-saas-images
GCS_CREDENTIALS_PATH=
GCS_BASE_URL=https://storage.googleapis.com

# Backend Configuration (for deployment)
SPRING_PROFILES_ACTIVE=prod

# Frontend Configuration
NODE_ENV=production
NEXT_PUBLIC_API_URL=https://your-backend-url.run.app

# Local Development (optional - docker-compose will override)
# DATABASE_URL=jdbc:postgresql://localhost:5432/hotelsaas
# PORT=8080
